/** Curva de easing padrão do painel — rápida e sem bounce (registo "product"). */
export const EASE: [number, number, number, number] = [0.22, 0.61, 0.36, 1];

/** Durações-base: o painel é uma ferramenta de trabalho, não uma página de marca. */
export const DURATION = {
  fast: 0.12,
  base: 0.18,
  slow: 0.24,
};
